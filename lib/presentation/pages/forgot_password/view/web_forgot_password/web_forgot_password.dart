import 'package:flutter/material.dart';

class WebForgotPassword extends StatelessWidget {
  const WebForgotPassword({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}